// Re-export from projects directory
// Individual projects are now in separate files in ./projects/
// This allows each project to have its own structure, formatting, and assets
export { projectData, projectData as default } from './projects/index.js';
