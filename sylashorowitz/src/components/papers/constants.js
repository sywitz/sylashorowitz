/**
 * Papers component constants
 * Centralized configuration values
 */

// CSS selector for paper cards - must match data-paper-index attribute in PaperCard
export const PAPER_SELECTOR = '[data-paper-index]';

// Scroll behavior for programmatic scrolling (smooth animation, align to top)
export const SCROLL_BEHAVIOR = {
  behavior: 'smooth',
  block: 'start'
};

